const n=+gets();
const k=+gets();
let arr=[];
for(let i=0;i<n;i+=1){
    arr[i]=+gets();
    }
    arr.sort((x,y)=>y-x);
    let result=arr.slice(0,k);
    let sum=0;
    for(let i=0;i<k;i+=1){
    sum+=result[i];
    }
    print(sum);
