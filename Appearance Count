/*.
Write a method that counts how many times given number appears in a given array. 
Write a test program to check if the method is working correctly.

*/


let len = +gets();
let arr = gets().split(' ');
let find = +gets();
let result = arr.filter((x) => x == find);
print(result.length);
