const size = +gets();
let counter = 0;
let arr = gets().split(' ').map(Number);
for (let i = 1; i < size - 1; i += 1) {
    if (arr[i] > arr[i - 1] && arr[i] > arr[i + 1]) {
        counter += 1;
    }
}
print(counter);
