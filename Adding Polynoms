const size = +gets();
let result = [];
let poly1 = gets().split(' ').map(Number);
let poly2 = gets().split(' ').map(Number);
for (let i = 0; i < size; i += 1) {
    result[i] = poly1[i] + poly2[i];
}
print(result.join(' '));
